site/
docs/
__pycache__/
*.pyc
.DS_Store
.obsidian/
.claude/skills-workspace/
venv/

# eval harness: cached article text (may be copyrighted full text) and
# per-run outputs (regenerable, and can be large across many model x article
# pairs) — never committed. Keep eval/corpus/manifest.json and eval/README.md.
eval/corpus/cache/
eval/corpus/.discovery_cache.json
eval/corpus/manifest_*.json
eval/corpus/doi_resolution_cache.json
eval/runs/

# wiki_health_check.py's dated reports (regenerable) and its run-history
# log (local trend data, not wiki content) — see scripts/wiki_health_check.py
eval/health/
